import 'package:flutter/material.dart';

class ShowMoreCredits extends StatelessWidget {
  const ShowMoreCredits({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
